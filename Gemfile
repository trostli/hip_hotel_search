source 'https://rubygems.org'

gem "sinatra"
gem 'sinatra-activerecord'

gem 'rake'

# A better web server
gem "puma"

# Allows for making parallelized HTTP requests using threads
gem "typhoeus"

# To load env variables
gem 'dotenv'

group :development do
  gem 'shotgun'
  gem 'tux'
  gem 'byebug'
end